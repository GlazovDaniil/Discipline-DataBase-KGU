SELECT b.maker, type, (b.red/a.red*100) src1 FROM
  (SELECT maker, count(type)/1.0 red
  FROM Product
  GROUP BY maker) a join
  (SELECT maker, type, count(type)/1.0 red
  FROM Product 
  GROUP BY maker, type) b
ON a.maker = b.maker



SELECT maker, type, 
CASE
WHEN src1 Is Null THEN 0.00
ELSE src1
END
FROM
(SELECT B.maker, B.type, CAST(src1 AS DECIMAL(9,2)) src1 FROM
(SELECT b.maker, type, (b.red/a.red*100) src1 FROM
   (SELECT maker, count(type)/1.0 red
   FROM Product
   GROUP BY maker) a join
   (SELECT maker, type, count(type)/1.0 red
   FROM Product 
   GROUP BY maker, type) b
 ON a.maker = b.maker) A
RIGHT JOIN
 (SELECT * FROM 
   (SELECT DISTINCT maker FROM Product) a
 CROSS JOIN
   (SELECT DISTINCT type FROM Product) b) B
ON A.maker = B.maker AND A.type = B.type) S



=========================
8 задача
=========================
В первом подзапросе собираем изготовителей, модели которых есть в табл PC 
и тип которых в Product "PC"
Во втором подзапросе собираются изгатовители, модели которых есть в табл Laptop
и тип лаптоп в Product 
Результат формируется через левое соединение с исключением пересечения и 
остальных данных из второго подзапроса

=========================
18 задача
=========================
В первом подзапросе собираем изготовителей и прайсы цветных принтеров
Во втором подзапросе находим минимальный прайс среди цветных принтеров
Результат формируется путем нахождения пересечения данных из двух подзапросов,
по ключу цене (прайсу)

=========================
58 задача
=========================
В первом подзапросе собираем изготовителей, типы и процентное соотношение,
округленое до двух знаков после запятой
  вся информация берется из результата соединения двух поздапросов, соедененных по производителям:
  1) собираются производители и колличество всех типов
  2) собираются производители, типы и колличество этих типов
Во втором подзапросе перемножаются производители и типы продукции
  вся информация берется из результата соединения двух поздапросов:
  1) производители
  2) типы продукции
Через провое соединение соединяются два подзапроса по производителю и типу
Результатом выступает производитель, тип продукции,  процентное отношение, сформированное
путем использования кейсов: если нет продукта, то процент 0.00